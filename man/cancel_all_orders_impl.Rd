% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impl_spottrading_orders_cancel_order.R
\name{cancel_all_orders_impl}
\alias{cancel_all_orders_impl}
\title{Cancel All Orders (Implementation)}
\usage{
cancel_all_orders_impl(
  keys = get_api_keys(),
  base_url = get_base_url(),
  .__coro_env_parent__ = <environment>
)
}
\arguments{
\item{keys}{List; API configuration parameters (e.g., from \code{get_api_keys()}). Contains API key,
secret, passphrase, and key version. Defaults to \code{get_api_keys()}.}

\item{base_url}{Character string; base URL for the KuCoin API (e.g., "https://api.kucoin.com").
Defaults to \code{get_base_url()}.}
}
\value{
Promise resolving to a \code{data.table} with:
\itemize{
\item \code{succeedSymbols} (character): Vector of symbols for which orders were successfully canceled.
\item \code{failedSymbols} (list): List of objects with \code{symbol} (character) and \code{error} (character)
for symbols where cancellation failed.
}
}
\description{
Cancels all spot orders across all symbols on the KuCoin Spot trading system asynchronously.
This endpoint sends a cancellation request for all open spot orders and returns a success indicator
if the request is accepted. Note that this endpoint only initiates cancellation; the actual
status of individual orders must be checked via order status endpoints or WebSocket subscription.
}
\section{Description}{

This function interacts with the KuCoin API endpoint \code{DELETE /api/v1/hf/orders/cancelAll}
to cancel all spot orders for the authenticated account, regardless of symbol. It is useful
for quickly clearing all open orders, such as during portfolio rebalancing or emergency stops.
}

\section{Differences from Other Cancellation Endpoints}{

\itemize{
\item \strong{Cancel Order By OrderId}: Cancels a single order using its unique order ID.
\item \strong{Cancel Order By ClientOid}: Cancels a single order using a client-assigned ID.
\item \strong{Cancel Partial Order}: Cancels a portion of a single order.
\item \strong{Cancel All Orders By Symbol}: Cancels all orders for a specific symbol.
\item \strong{Cancel All Orders}: Cancels \strong{all} spot orders across all symbols in one request,
offering maximum efficiency for bulk cancellations.
}

Use this function when you need to terminate all spot trading activity instantly.
}

\section{Workflow}{

\enumerate{
\item \strong{Authentication}: Generates private API headers using the provided \code{keys}.
\item \strong{API Request}: Sends a DELETE request to the KuCoin API with a 3-second timeout.
\item \strong{Response Processing}: Parses the response and returns successful and failed symbols
if the request is accepted.
}
}

\section{API Details}{

\itemize{
\item \strong{Endpoint}: \verb{DELETE https://api.kucoin.com/api/v1/hf/orders/cancelAll}
\item \strong{Domain}: Spot
\item \strong{API Channel}: Private
\item \strong{API Permission}: Spot
\item \strong{Rate Limit Pool}: Spot
\item \strong{Rate Limit Weight}: 30
\item \strong{SDK Details}: Service: Spot, Sub-service: Order, Method: cancelAllOrders
\item \strong{Official Documentation}: \href{https://www.kucoin.com/docs-new/rest/spot-trading/orders/cancel-all-orders}{KuCoin Cancel All Orders}
}
}

\section{Examples}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{library(coro)
library(data.table)

main_async <- coro::async(function() \{
  # Cancel all spot orders
  result <- await(cancel_all_orders_impl())
  print(result)
\})

# Run the async function
main_async()
while (!later::loop_empty()) later::run_now()
}\if{html}{\out{</div>}}

\strong{Expected Output}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{   succeedSymbols failedSymbols
1:    ETH-USDT            []
2:    BTC-USDT            []
}\if{html}{\out{</div>}}
}

